# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()

GET     /requests                   controllers.Requests.index()
GET     /requests/new               controllers.Requests.newForm()
POST    /requests/create            controllers.Requests.create()
GET     /requests/show/:id          controllers.Requests.show(id: Long)

# Login
GET     /loginForm                  controllers.Authentication.loginForm()
POST    /login                      controllers.Authentication.login()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
